version: "2"
services:
  db:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: YOUR_MYSQL_PASSWORD_GOES_HERE_AND_IS_DEFINED_THE_SAME_IN_ENVIRONMENT_DOT_PHP
    ports:
      - "3306:3306"
  rboxlo:
    build: ./
    ports:
      - "80:80"
    volumes:
      - ./environment/nginx/logs:/var/log/nginx
      - ./environment/site:/var/www/data